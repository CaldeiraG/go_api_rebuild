# SQL Server connection used by the API (all lines, including GEN5).
DB_HOST=
DB_PORT=
DB_USER=
DB_PASS=
DB_NAME=

# Optional: absolute path to prodFAssy_config.json.
# Defaults to the file next to the executable, then the project root.
# PROD_CONFIG_PATH=

# Optional: set to true to enable per-request access logging (off by default).
# LOG_REQUESTS=
